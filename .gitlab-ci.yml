stages:
  - build
  - run

build:
  stage: build
  script:
    - gfortran --version
    - g++ --version
    - cmake --version
    - git submodule init
    - git submodule update
    - ./setup -FC=gfortran -CXX=g++
    - cd build
    - make
  artifacts:
    paths: 
      - build/

run:
  stage: run
  script:
    - cd build
    - ctest
